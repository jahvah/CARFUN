ALTER TABLE stocks
ADD COLUMN ITEM_NAME VARCHAR(45) AFTER SUPPLIER_NAME,
ADD COLUMN ITEM_PRICE DECIMAL(10,2) AFTER ITEM_NAME;

ALTER TABLE expenses
ADD COLUMN ITEM_ID INT AFTER AMOUNT,
ADD COLUMN QUANTITY INT AFTER ITEM_ID;
